{
	"wait_first": 0.8,
	"steps": [
		{ "shot": "boot_ready_01.png" },
		{ "print": { "node": "%Title", "prop": "text" } },
		{ "assert": { "node": "%PlaySolo", "disabled": false } },
		{ "assert": { "node": "%AfterPlay", "visible": false } },
		{ "assert": { "node": "%Status", "visible": true } }
	]
}
